"use client";

// Re-export the platform hook so consumers can import from the hooks folder per the search-platform
// structure. The implementation lives with the provider.
export { useEnterpriseSearch } from "../EnterpriseSearchProvider";
